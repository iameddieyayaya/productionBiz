import React from "react"
import Header from "../components/header"

export default () => (
  <div style={{ color: "teal" }}>
    <Header siteTitle="About Page Title" />
    <p>Sush wow, very React!</p>
  </div>
)
